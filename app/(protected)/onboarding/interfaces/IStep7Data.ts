// app/(protected)/onboarding/interfaces/IStep7Data.ts

/**
 * Typically, Step 7 doesn't store data, it's just success/CTA.
 * But if you do store final confirmations, define them here.
 */
export interface IStep7Data {
    completed: boolean;
  }
  